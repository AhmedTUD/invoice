# تعليمات النشر - FSMI TV & HA Invoice System

## 🎯 معلومات المشروع
- **الدومين**: http://invoice.smart-sense.site
- **GitHub**: https://github.com/AhmedTUD/invoice.git
- **النوع**: Progressive Web App (PWA)

## 📋 خطوات النشر الكاملة

### 1. رفع المشروع إلى GitHub
```bash
# على جهازك المحلي
chmod +x git-setup.sh
./git-setup.sh
```

### 2. النشر على السيرفر
```bash
# على السيرفر (عبر SSH)
wget https://raw.githubusercontent.com/AhmedTUD/invoice/main/server-deploy.sh
chmod +x server-deploy.sh
./server-deploy.sh
```

### 3. التحقق من النشر
- افتح: http://invoice.smart-sense.site
- لوحة التحكم: http://invoice.smart-sense.site/#/admin
- بيانات الدخول: admin / admin2025

## 🔄 التحديثات المستقبلية

### تحديث سريع:
```bash
# على السيرفر
cd /opt/invoice
./update-deploy.sh
```

### تحديث كامل:
```bash
# على السيرفر
cd /opt/invoice
git pull origin main
docker-compose up --build -d
```

## 📱 للموظفين

### كيفية الوصول:
1. افتح الرابط: http://invoice.smart-sense.site
2. على الموبايل: انقر "تثبيت التطبيق" عند ظهور الرسالة
3. على iPhone: Safari → مشاركة → إضافة للشاشة الرئيسية

### الاستخدام:
1. أدخل بياناتك الأساسية (تُحفظ تلقائياً)
2. أضف فواتيرك مع الصور
3. اختر الموديل من القائمة
4. أرسل البيانات

## 👨‍💼 للمشرفين

### الوصول للوحة التحكم:
1. اذهب إلى: http://invoice.smart-sense.site/#/admin
2. سجل دخولك: admin / admin2025
3. **مهم**: غيّر كلمة المرور فوراً!

### الميزات المتاحة:
- عرض وفلترة جميع البيانات
- تصدير Excel مع الصور
- تصدير ZIP منظم
- إدارة الموديلات
- حذف البيانات (مفلتر/شامل)
- إحصائيات تفصيلية

## 🔧 إدارة السيرفر

### أوامر مفيدة:
```bash
# عرض حالة الحاويات
cd /opt/invoice && docker-compose ps

# عرض السجلات
cd /opt/invoice && docker-compose logs -f

# إعادة تشغيل
cd /opt/invoice && docker-compose restart

# إيقاف التطبيق
cd /opt/invoice && docker-compose down

# نسخة احتياطية
/opt/backup-invoice.sh
```

### مراقبة الأداء:
```bash
# استخدام الموارد
docker stats

# مساحة القرص
df -h

# سجلات Nginx
sudo tail -f /var/log/nginx/invoice-smart-sense.access.log
```

## 🆘 استكشاف الأخطاء

### التطبيق لا يعمل:
```bash
# فحص الحاويات
docker-compose ps

# فحص السجلات
docker-compose logs

# إعادة البناء
docker-compose down
docker-compose up --build -d
```

### مشاكل الدومين:
1. تأكد من إعداد DNS للدومين invoice.smart-sense.site
2. تحقق من إعداد Nginx: `sudo nginx -t`
3. فحص السجلات: `sudo tail -f /var/log/nginx/error.log`

### مشاكل SSL:
```bash
# تجديد الشهادة
sudo certbot renew

# فحص الشهادة
sudo certbot certificates
```

## 📊 النسخ الاحتياطية

### تلقائية:
- تتم يومياً في الساعة 2 صباحاً
- تُحفظ في: `/opt/backups/invoice/`
- تُحذف النسخ الأقدم من 7 أيام

### يدوية:
```bash
# عمل نسخة احتياطية
/opt/backup-invoice.sh

# استعادة نسخة احتياطية
cd /opt/invoice
docker cp backup-20260119.tar.gz invoice-backend:/tmp/
docker exec invoice-backend tar xzf /tmp/backup-20260119.tar.gz -C /
docker-compose restart
```

## 🔒 الأمان

### تغيير كلمة مرور المشرف:
1. سجل دخول لوحة التحكم
2. انقر "تغيير كلمة المرور"
3. أدخل كلمة مرور قوية جديدة

### تحديثات الأمان:
```bash
# تحديث النظام
sudo apt update && sudo apt upgrade -y

# تحديث Docker
sudo apt update && sudo apt install docker-ce docker-ce-cli containerd.io
```

## 📞 الدعم

### معلومات الاتصال:
- **المطور**: Ahmed TUD
- **GitHub Issues**: https://github.com/AhmedTUD/invoice/issues
- **الموقع**: http://invoice.smart-sense.site

### في حالة المشاكل:
1. تحقق من السجلات أولاً
2. أعد تشغيل الخدمات
3. تأكد من تحديث النظام
4. اتصل بالدعم التقني

---

**آخر تحديث**: 19 يناير 2026  
**الحالة**: ✅ جاهز للإنتاج  
**الدومين**: http://invoice.smart-sense.site