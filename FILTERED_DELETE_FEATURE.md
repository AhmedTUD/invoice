# ميزة الحذف المفلتر - FSMI TV & HA By SmartSense

## نظرة عامة
تم تطوير نظام حذف ذكي يتيح للمشرفين حذف البيانات بناءً على الفلاتر المطبقة، مما يوفر مرونة أكبر في إدارة البيانات.

## الميزات الجديدة

### 1. الحذف الذكي
- **حذف مفلتر**: حذف البيانات التي تطابق الفلاتر المطبقة فقط
- **حذف شامل**: حذف جميع البيانات إذا لم تكن هناك فلاتر مطبقة
- **مؤشر بصري**: عرض عدد السجلات التي سيتم حذفها في زر الحذف

### 2. رسائل تأكيد تفصيلية
- **تأكيد مفصل**: عرض الفلاتر المطبقة وعدد السجلات المتأثرة
- **معاينة الحذف**: إظهار ما سيتم حذفه بالضبط قبل التأكيد
- **حماية من الحذف الخاطئ**: رسائل تحذيرية واضحة

### 3. مؤشرات بصرية محسنة
- **شارة الفلترة**: عرض عدد السجلات المفلترة من إجمالي السجلات
- **زر ديناميكي**: تغيير نص زر الحذف ليعكس نوع العملية
- **حالة التحميل**: مؤشر تحميل أثناء عملية الحذف

## كيفية العمل

### 1. بدون فلاتر (حذف شامل)
```
عند عدم تطبيق أي فلاتر:
- يظهر الزر: "حذف الكل (العدد الإجمالي)"
- رسالة التأكيد تشمل تحذير من حذف جميع البيانات
- يتم حذف: الفواتير + الطلبات + الموظفين + الملفات
```

### 2. مع فلاتر مطبقة (حذف مفلتر)
```
عند تطبيق فلاتر:
- يظهر الزر: "حذف المفلتر (عدد السجلات المفلترة)"
- رسالة التأكيد تعرض الفلاتر المطبقة
- يتم حذف: السجلات التي تطابق الفلاتر فقط
```

## التحديثات التقنية

### 1. API Endpoint جديد
```javascript
DELETE /api/submissions/filtered
Body: {
  sessionToken: string,
  filters: {
    name?: string,
    serial?: string, 
    store?: string,
    model?: string
  }
}
```

### 2. منطق الحذف المفلتر
```sql
-- بناء استعلام ديناميكي بناءً على الفلاتر
SELECT DISTINCT i.id as invoiceId, i.filePath, s.id as submissionId
FROM submissions s
LEFT JOIN invoices i ON s.id = i.submissionId
WHERE [شروط الفلترة الديناميكية]

-- حذف الفواتير المطابقة
DELETE FROM invoices WHERE id IN (معرفات الفواتير)

-- حذف الطلبات المطابقة  
DELETE FROM submissions WHERE id IN (معرفات الطلبات)

-- حذف الملفات المرتبطة
```

### 3. تحسينات الواجهة
- **مؤشر الفلترة**: `مفلتر: X من Y` 
- **زر ديناميكي**: يتغير النص حسب حالة الفلترة
- **رسائل تفاعلية**: تأكيدات مخصصة لكل حالة

## أمثلة الاستخدام

### مثال 1: حذف فواتير موظف معين
```
1. تطبيق فلتر الاسم: "أحمد محمد"
2. النتيجة: 5 فواتير مفلترة
3. النقر على "حذف المفلتر (5)"
4. التأكيد: سيتم حذف 5 فواتير لأحمد محمد
5. النتيجة: حذف فواتير أحمد فقط
```

### مثال 2: حذف فواتير فرع معين
```
1. تطبيق فلتر الفرع: "فرع القاهرة"
2. النتيجة: 25 فاتورة مفلترة
3. النقر على "حذف المفلتر (25)"
4. التأكيد: سيتم حذف 25 فاتورة من فرع القاهرة
5. النتيجة: حذف فواتير الفرع فقط
```

### مثال 3: حذف فواتير موديل معين
```
1. تطبيق فلتر الموديل: "RS68AB820B1/MR"
2. النتيجة: 8 فواتير مفلترة
3. النقر على "حذف المفلتر (8)"
4. التأكيد: سيتم حذف 8 فواتير للموديل المحدد
5. النتيجة: حذف فواتير الموديل فقط
```

## الأمان والحماية

### 1. التحقق من الصلاحيات
- جميع عمليات الحذف تتطلب جلسة مشرف صالحة
- التحقق من صحة الجلسة قبل كل عملية حذف

### 2. رسائل التأكيد المفصلة
- عرض عدد السجلات التي سيتم حذفها
- إظهار الفلاتر المطبقة بوضوح
- تحذيرات واضحة للعمليات الشاملة

### 3. تسجيل العمليات
- تسجيل تفصيلي لعمليات الحذف في وحدة التحكم
- عرض إحصائيات الحذف (فواتير، طلبات، ملفات)
- رسائل نجاح/فشل واضحة

## واجهة المستخدم

### 1. مؤشر الفلترة
```
عند تطبيق فلاتر:
[الفلترة والبحث] [مفلتر: 15 من 100] [مسح الفلاتر]
```

### 2. زر الحذف الديناميكي
```
بدون فلاتر: [🗑️ حذف الكل (100)]
مع فلاتر:   [🗑️ حذف المفلتر (15)]
أثناء التحميل: [⟳ حذف المفلتر (15)]
```

### 3. رسائل التأكيد
```
للحذف المفلتر:
"تحذير: سيتم حذف البيانات المفلترة فقط (15 سجل).

الفلاتر المطبقة:
- الاسم: أحمد
- الفرع: القاهرة

هل أنت متأكد من حذف هذه البيانات؟"

للحذف الشامل:
"تحذير: سيتم حذف جميع البيانات (100 سجل).

هذا سيحذف:
- جميع الفواتير
- جميع الطلبات  
- جميع بيانات الموظفين
- جميع الملفات المرفوعة

هل أنت متأكد؟"
```

## الفوائد

### 1. للمشرفين
- **مرونة أكبر**: حذف بيانات محددة دون التأثير على الباقي
- **أمان محسن**: تأكيدات واضحة تمنع الحذف الخاطئ
- **كفاءة أعلى**: حذف سريع للبيانات غير المرغوبة

### 2. لإدارة البيانات
- **تنظيف انتقائي**: إزالة بيانات معينة دون المساس بالأخرى
- **صيانة أسهل**: تنظيف دوري للبيانات القديمة أو الخاطئة
- **مساحة محسنة**: توفير مساحة التخزين بحذف البيانات غير الضرورية

### 3. للنظام
- **أداء محسن**: قاعدة بيانات أصغر وأسرع
- **نسخ احتياطية أصغر**: حجم أقل للبيانات المحفوظة
- **صيانة أقل**: تقليل الحاجة لتنظيف البيانات يدوياً

## حالات الاستخدام العملية

### 1. تنظيف البيانات التجريبية
```
الهدف: حذف البيانات التجريبية فقط
الطريقة: فلترة بالإيميل "test@example.com" ثم الحذف
النتيجة: حذف البيانات التجريبية دون المساس بالبيانات الحقيقية
```

### 2. إزالة فواتير موظف مُستقيل
```
الهدف: حذف جميع فواتير موظف معين
الطريقة: فلترة بالاسم أو الكود ثم الحذف
النتيجة: إزالة بيانات الموظف دون التأثير على الآخرين
```

### 3. تنظيف فواتير فترة معينة
```
الهدف: حذف فواتير قديمة أو خاطئة
الطريقة: فلترة بالتاريخ أو الموديل ثم الحذف
النتيجة: تنظيف البيانات القديمة مع الحفاظ على الحديثة
```

### 4. إعادة تعيين فرع معين
```
الهدف: حذف جميع بيانات فرع معين
الطريقة: فلترة بالفرع ثم الحذف
النتيجة: إعادة تعيين الفرع دون التأثير على الفروع الأخرى
```

## ملاحظات مهمة

1. **النسخ الاحتياطية**: يُنصح بعمل نسخة احتياطية قبل عمليات الحذف الكبيرة
2. **الحذف النهائي**: عمليات الحذف نهائية ولا يمكن التراجع عنها
3. **الملفات المرتبطة**: يتم حذف الملفات المرفوعة تلقائياً مع البيانات
4. **إعادة الفلترة**: يتم مسح الفلاتر تلقائياً بعد الحذف المفلتر
5. **التحديث التلقائي**: يتم تحديث العرض تلقائياً بعد الحذف

---

**تاريخ الإنشاء**: 19 يناير 2026  
**الإصدار**: 1.0  
**المطور**: Kiro AI Assistant