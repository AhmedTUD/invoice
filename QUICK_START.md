# دليل البدء السريع - FSMI TV & HA By SmartSense

## 🚀 تشغيل النظام

### الخطوة 1: تثبيت التبعيات
```bash
npm install --legacy-peer-deps
```

### الخطوة 2: تشغيل النظام
```bash
npm run dev
```

### الخطوة 3: فتح التطبيق
افتح المتصفح واذهب إلى: **http://localhost:3000**

---

## ✅ ما تم تغييره

### قبل التحديث (localStorage):
- ❌ البيانات تُحفظ في المتصفح فقط
- ❌ الصور تُحفظ كـ Base64 (تستهلك ذاكرة كبيرة)
- ❌ قيود على حجم البيانات
- ❌ البيانات تُفقد عند مسح المتصفح

### بعد التحديث (SQLite):
- ✅ قاعدة بيانات SQLite محلية
- ✅ الصور تُحفظ كملفات على القرص الصلب
- ✅ لا توجد قيود على حجم البيانات
- ✅ البيانات محفوظة بشكل دائم
- ✅ يعمل على جميع الأجهزة

---

## 📁 مواقع الملفات

- **قاعدة البيانات**: `server/fsmi_database.sqlite`
- **الصور المرفوعة**: `server/uploads/`
- **الخادم**: يعمل على المنفذ 3001
- **واجهة المستخدم**: تعمل على المنفذ 3000

---

## 🔐 بيانات الدخول للمشرفين

- **اسم المستخدم**: admin
- **كلمة المرور**: admin2025

---

## 🛠️ استكشاف الأخطاء

### إذا ظهرت رسالة خطأ "خطأ في الاتصال بالخادم":
1. تأكد من تشغيل الأمر `npm run dev`
2. تأكد من أن المنفذ 3001 غير مستخدم
3. انتظر قليلاً حتى يبدأ الخادم

### إذا لم تظهر البيانات في لوحة التحكم:
1. تحقق من وحدة التحكم في المتصفح (F12)
2. تأكد من وجود ملف `server/fsmi_database.sqlite`
3. جرب إعادة تشغيل النظام

---

## 📊 اختبار النظام

1. اذهب إلى الصفحة الرئيسية
2. املأ البيانات الأساسية
3. أضف فاتورة واحدة على الأقل
4. ارفع صورة للفاتورة
5. اضغط "إرسال البيانات"
6. اذهب إلى لوحة التحكم للتأكد من حفظ البيانات

---

## 🎯 المميزات الجديدة

- **أداء أفضل**: لا توجد قيود على حجم البيانات
- **استقرار أكبر**: لا تعتمد على ذاكرة المتصفح
- **توافق شامل**: يعمل على Windows, Mac, Linux
- **حفظ دائم**: البيانات لا تُفقد أبداً
- **ملفات منظمة**: الصور محفوظة في مجلد منفصل