# FSMI TV & HA By SmartSense - Invoice Tracking System

نظام تتبع حوافز FSMI TV & HA By SmartSense - تطبيق ويب تقدمي (PWA) قوي لتتبع الحوافز والفواتير مع قاعدة بيانات SQLite.

## 🌐 الرابط المباشر
**[http://invoice.smart-sense.site](http://invoice.smart-sense.site)**

## ✨ الميزات الرئيسية

### 📱 تطبيق ويب تقدمي (PWA)
- يعمل على جميع الأجهزة (موبايل، تابلت، ديسكتوب)
- إمكانية التثبيت كتطبيق أصلي
- يعمل بدون إنترنت بعد التثبيت الأول
- تحديثات تلقائية

### 👥 للموظفين
- إدخال بيانات الموظف مرة واحدة مع حفظ تلقائي
- إضافة فواتير متعددة بسهولة
- رفع صور الفواتير (JPG, PNG, PDF)
- بحث ذكي في الموديلات
- معاينة الصور قبل الإرسال
- واجهة متجاوبة وسهلة الاستخدام

### 👨‍💼 للمشرفين
- لوحة تحكم شاملة لعرض جميع البيانات
- فلترة متقدمة (اسم، كود، فرع، موديل)
- تصدير Excel متقدم مع صور عالية الجودة
- تصدير ZIP منظم بالفروع والموظفين
- إدارة الموديلات (إضافة، تعديل، حذف)
- حذف ذكي للبيانات (مفلتر أو شامل)
- تغيير كلمة المرور
- إحصائيات تفصيلية

### 🔒 الأمان
- جلسات آمنة مع انتهاء صلاحية
- تشفير كلمات المرور
- حماية من الوصول غير المصرح
- نسخ احتياطية تلقائية

## 🚀 التثبيت والتشغيل

### متطلبات النظام
- Node.js 18+
- SQLite3
- Docker (للنشر)

### التشغيل المحلي
```bash
# استنساخ المشروع
git clone https://github.com/AhmedTUD/invoice.git
cd invoice

# تثبيت المكتبات
npm install

# تشغيل التطبيق
npm run dev
```

### النشر باستخدام Docker
```bash
# النشر السريع
./quick-deploy.sh

# النشر الكامل مع الدومين
./deploy.sh invoice.smart-sense.site
```

## 📱 كيفية الاستخدام

### للموظفين:
1. افتح الرابط: [http://invoice.smart-sense.site](http://invoice.smart-sense.site)
2. أدخل بياناتك الأساسية (تُحفظ تلقائياً)
3. أضف فواتيرك مع الصور
4. اختر الموديل من القائمة الذكية
5. أرسل البيانات

### للمشرفين:
1. اذهب إلى: [http://invoice.smart-sense.site/#/admin](http://invoice.smart-sense.site/#/admin)
2. سجل دخولك: admin / admin2025
3. استعرض وفلتر البيانات
4. صدّر التقارير (Excel/ZIP)
5. أدر الموديلات والإعدادات

### تثبيت التطبيق على الموبايل:

#### Android (Chrome):
1. افتح الرابط في Chrome
2. انقر "تثبيت التطبيق" عند ظهور الرسالة
3. أو من قائمة Chrome → "إضافة إلى الشاشة الرئيسية"

#### iPhone (Safari):
1. افتح الرابط في Safari
2. انقر زر المشاركة (المربع مع السهم)
3. اختر "إضافة إلى الشاشة الرئيسية"

## 🏗️ البنية التقنية

### Frontend
- **React 19** مع TypeScript
- **Tailwind CSS** للتصميم المتجاوب
- **Vite** لبناء التطبيق
- **PWA** مع Service Worker
- **ExcelJS** لتصدير Excel
- **JSZip** لتصدير الملفات المضغوطة

### Backend
- **Node.js** مع Express
- **SQLite** قاعدة بيانات محلية
- **Multer** لرفع الملفات
- **UUID** للمعرفات الفريدة

### DevOps
- **Docker** للحاويات
- **Nginx** كخادم ويب وproxy
- **Let's Encrypt** لشهادات SSL
- **GitHub Actions** للنشر التلقائي

## 📊 قاعدة البيانات

### الجداول الرئيسية:
- `employees` - بيانات الموظفين
- `submissions` - طلبات الحوافز
- `invoices` - تفاصيل الفواتير
- `models` - موديلات المنتجات
- `admin_sessions` - جلسات المشرفين
- `admin_settings` - إعدادات النظام

## 🔧 الإعدادات

### بيانات الدخول الافتراضية:
- **المشرف**: admin / admin2025

### المنافذ:
- **Frontend**: 3000 (تطوير) / 80 (إنتاج)
- **Backend**: 3001

### المجلدات المهمة:
- `server/data/` - قاعدة البيانات
- `server/uploads/` - الملفات المرفوعة
- `public/icons/` - أيقونات PWA

## 📈 الإحصائيات والتقارير

### تقارير Excel:
- شيت ملخص عام لجميع الفروع
- شيت منفصل لكل فرع مع الإحصائيات
- إدراج الصور بجودة عالية
- تنسيق احترافي مع ألوان وحدود

### ملفات ZIP:
- تنظيم هرمي: الفرع → الموظف → الموديل
- أسماء ملفات ذكية مع التاريخ
- ملف README تفصيلي
- ضغط محسن للحجم

## 🛠️ التطوير والمساهمة

### هيكل المشروع:
```
invoice/
├── public/          # ملفات PWA والأيقونات
├── src/
│   ├── components/  # مكونات React
│   ├── pages/       # صفحات التطبيق
│   ├── services/    # خدمات API
│   └── types/       # تعريفات TypeScript
├── server/          # خادم Node.js
├── docker/          # ملفات Docker
└── docs/           # التوثيق
```

### أوامر مفيدة:
```bash
# تطوير
npm run dev          # تشغيل التطوير
npm run build        # بناء الإنتاج
npm run preview      # معاينة البناء

# Docker
docker-compose up -d # تشغيل الحاويات
docker-compose logs  # عرض السجلات
docker-compose down  # إيقاف الحاويات

# قاعدة البيانات
sqlite3 server/data/fsmi_database.sqlite
```

## 🆘 الدعم واستكشاف الأخطاء

### مشاكل شائعة:
1. **التطبيق لا يعمل**: تحقق من تشغيل الخادم على المنفذ 3001
2. **الصور لا تظهر**: تأكد من صلاحيات مجلد uploads
3. **مشاكل قاعدة البيانات**: تحقق من وجود ملف SQLite

### السجلات:
```bash
# سجلات التطبيق
docker-compose logs -f

# سجلات Nginx
sudo tail -f /var/log/nginx/access.log
```

## 📞 معلومات الاتصال

- **المطور**: Ahmed TUD
- **GitHub**: [https://github.com/AhmedTUD/invoice](https://github.com/AhmedTUD/invoice)
- **الموقع**: [http://invoice.smart-sense.site](http://invoice.smart-sense.site)

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT - انظر ملف [LICENSE](LICENSE) للتفاصيل.

## 🙏 شكر وتقدير

- فريق SmartSense للدعم والتطوير
- مجتمع React و Node.js للأدوات الرائعة
- جميع المساهمين في المشروع

---

**آخر تحديث**: 19 يناير 2026  
**الإصدار**: 1.0.0  
**الحالة**: ✅ جاهز للإنتاج

## المميزات الجديدة

- ✅ قاعدة بيانات SQLite محلية بدلاً من localStorage
- ✅ خادم Node.js مع Express
- ✅ رفع وحفظ الملفات على القرص الصلب
- ✅ API RESTful للتواصل بين Frontend و Backend
- ✅ يعمل على جميع أنظمة التشغيل (Windows, Mac, Linux)
- ✅ لا يحتاج اتصال بالإنترنت بعد التثبيت

## متطلبات التشغيل

- Node.js (الإصدار 18 أو أحدث)
- npm أو yarn

## التثبيت والتشغيل

### 1. تثبيت التبعيات
```bash
npm install
```

### 2. تشغيل النظام
```bash
npm run dev
```

هذا الأمر سيقوم بتشغيل:
- الخادم (Backend) على المنفذ 3001
- واجهة المستخدم (Frontend) على المنفذ 3000

### 3. فتح التطبيق
افتح المتصفح واذهب إلى: `http://localhost:3000`

## بنية المشروع

```
├── server/                 # خادم Node.js
│   ├── index.js           # الخادم الرئيسي
│   ├── database.js        # إعداد قاعدة البيانات
│   ├── uploads/           # مجلد الملفات المرفوعة
│   └── fsmi_database.sqlite # قاعدة البيانات
├── services/              # خدمات Frontend
│   ├── apiService.ts      # خدمة API الجديدة
│   └── mockDb.ts          # محول للتوافق مع الكود القديم
├── pages/                 # صفحات التطبيق
├── components/            # مكونات React
└── ...
```

## قاعدة البيانات

يستخدم النظام قاعدة بيانات SQLite مع جدولين:

### جدول submissions
- id (TEXT PRIMARY KEY)
- email, name, mobile, serial
- storeName, storeCode
- createdAt

### جدول invoices
- id (TEXT PRIMARY KEY)
- submissionId (FOREIGN KEY)
- model, salesDate
- fileName, filePath

## API Endpoints

- `POST /api/submissions` - حفظ طلب جديد
- `GET /api/submissions` - جلب جميع الطلبات
- `DELETE /api/submissions` - مسح جميع البيانات
- `POST /api/test-data` - إضافة بيانات تجريبية

## الملفات

- يتم حفظ الملفات في مجلد `server/uploads/`
- يتم الوصول للملفات عبر `/uploads/filename`
- الحد الأقصى لحجم الملف: 10MB

## بيانات الدخول للمشرفين

- اسم المستخدم: `admin`
- كلمة المرور: `admin2025`

## الاختلافات عن النسخة السابقة

1. **قاعدة البيانات**: SQLite بدلاً من localStorage
2. **الملفات**: حفظ على القرص الصلب بدلاً من Base64
3. **الأداء**: أسرع وأكثر استقراراً
4. **السعة**: لا توجد قيود على حجم البيانات
5. **التوافق**: يعمل على جميع الأجهزة

## استكشاف الأخطاء

### إذا لم يعمل الخادم:
```bash
# تأكد من أن المنفذ 3001 غير مستخدم
netstat -an | findstr :3001

# أو جرب تشغيل الخادم منفصلاً
npm run server
```

### إذا لم تظهر البيانات:
- تأكد من تشغيل الخادم على المنفذ 3001
- تحقق من وحدة التحكم في المتصفح للأخطاء
- تأكد من وجود ملف قاعدة البيانات في `server/fsmi_database.sqlite`

## التطوير

لتطوير النظام:

1. **Frontend فقط**: `vite`
2. **Backend فقط**: `npm run server`
3. **كلاهما**: `npm run dev`